{%- liquid
  assign nav_list  = section.settings.menu | default: 'main-menu'
  assign nav_links = linklists[nav_list].links
-%}

<style>
  /* ================================================================
     Petrichor Atelier — Site Header
     Hex fallbacks on every token so the header renders even if
     petrichor-tokens.css hasn't been uploaded yet.
     ================================================================ */

  .pa-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 300;
    background: var(--pa-ivory, #f9f8f4);
    color: var(--pa-ink, #1d1d1b);
    border-bottom: 1px solid var(--border-soft, #e3ddd1);
    transition:
      background      240ms ease,
      border-color    240ms ease,
      backdrop-filter 240ms ease;
  }

  .pa-header.is-scrolled {
    background: rgba(249, 248, 244, 0.88);
    border-bottom-color: var(--border-soft, #e3ddd1);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }

  /* Transparent variant — used when hero banner is first section */
  .pa-header.pa-header--transparent {
    background: transparent;
    border-bottom-color: transparent;
  }

  /* Switch text to ivory when floating over a dark hero */
  .pa-header--transparent:not(.is-scrolled) {
    color: var(--pa-ivory, #f9f8f4);
  }

  /* When the user has scrolled, always override the transparent state —
     this rule has higher specificity (0,3,0) than either class alone */
  .pa-header.pa-header--transparent.is-scrolled {
    background: rgba(249, 248, 244, 0.88);
    border-bottom-color: var(--border-soft, #e3ddd1);
    color: var(--pa-ink, #1d1d1b);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }

  .pa-header__inner {
    padding: 0 var(--gutter, 40px);
  }

  @media (max-width: 749px) {
    .pa-header__inner { padding: 0 var(--gutter-mobile, 20px); }
  }

  /* ── Row 1: utility nav ── */

  .pa-header__row--utility {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 24px;
    padding: 14px 0 12px;
  }

  .pa-header__nav,
  .pa-header__util {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .pa-header__util {
    justify-content: flex-end;
  }

  .pa-header__link {
    font-family: var(--font-body, 'Instrument Sans', 'Helvetica Neue', sans-serif);
    font-weight: 500;
    font-size: 12px;
    letter-spacing: 0.01em;
    color: inherit;
    text-decoration: none;
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    transition: opacity 160ms ease;
  }

  .pa-header__link:hover         { opacity: 0.55; }
  .pa-header__link[aria-current] { opacity: 0.45; }

  .pa-header__cart-count { opacity: 0.65; }

  /* ── Row 2: centred logo ── */

  .pa-header__row--logo {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 4px 0 18px;
  }

  .pa-header__logo-wrap {
    margin: 0;
    padding: 0;
    font-size: inherit;
    font-weight: inherit;
    line-height: 1;
  }

  .pa-header__logo-link {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    color: inherit;
  }

  .pa-header__logo {
    height: 18px;
    width: auto;
    display: block;
  }

  .pa-header__logo--light { display: none; }
  .pa-header--transparent:not(.is-scrolled) .pa-header__logo--dark { display: none; }
  .pa-header--transparent:not(.is-scrolled) .pa-header__logo--light { display: block; }

  /* Mobile logo slot — hidden on desktop */
  .pa-header__mob-logo { display: none; }

  /* CSS-native fallback: hero present → transparent until scrolled, frosted-glass after */
  body:has(.section-pa-hero) .pa-header:not(.is-scrolled) {
    background: transparent;
    border-bottom-color: transparent;
    color: var(--pa-ivory, #f9f8f4);
  }
  body:has(.section-pa-hero) .pa-header:not(.is-scrolled) .pa-header__logo--dark { display: none; }
  body:has(.section-pa-hero) .pa-header:not(.is-scrolled) .pa-header__logo--light { display: block; }

  body:has(.section-pa-hero) .pa-header.is-scrolled {
    background: rgba(249, 248, 244, 0.88);
    border-bottom-color: var(--border-soft, #e3ddd1);
    color: var(--pa-ink, #1d1d1b);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }
  body:has(.section-pa-hero) .pa-header.is-scrolled .pa-header__logo--dark { display: block; }
  body:has(.section-pa-hero) .pa-header.is-scrolled .pa-header__logo--light { display: none; }

  /* ── Mobile ── */

  .pa-header__mob-left,
  .pa-header__mob-right { display: none; }

  @media (max-width: 989px) {
    .pa-header__nav,
    .pa-header__util              { display: none; }
    .pa-header__row--utility      { grid-template-columns: auto 1fr auto; }
    .pa-header__row--logo         { display: none; }
    .pa-header__mob-left,
    .pa-header__mob-right         { display: flex; align-items: center; }
    .pa-header__mob-left          { gap: 18px; }
    .pa-header__mob-right         { gap: 18px; grid-column: 3; }
    .pa-header__mob-logo          { display: flex; justify-content: center; align-items: center; }
  }

  /* Push page content below the fixed header.
     80px is a safe fallback until JS measures the real height. */
  body { padding-top: var(--pa-header-height, 80px); }

  /* ================================================================
     PA Navigation Drawer
     ================================================================ */

  /* Scrim sits above header (z 300) so it truly blocks the page */
  .pa-drawer-scrim {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: rgba(48, 75, 61, 0.65);
    z-index: 400;
    opacity: 0;
    pointer-events: none;
    transition: opacity 420ms ease;
  }
  .pa-drawer-scrim.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  /* Drawer covers the full viewport — acts as both modal overlay and slide-in panel */
  .pa-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: 401;
    pointer-events: none;
  }
  .pa-drawer.is-open { pointer-events: auto; }

  /* Pine overlay — fades in/out as the pseudo-element of the full-screen drawer */
  .pa-drawer::before {
    content: '';
    position: absolute;
    top: 0; right: 0; bottom: 0; left: 0;
    background: rgb(48, 75, 61);
    opacity: 0;
    transition: opacity 360ms ease;
  }
  .pa-drawer.is-open::before { opacity: 0.6; }
  /* Dim on hover over pine area to signal it's clickable */
  .pa-drawer.is-open:hover::before { opacity: 0.45; transition-duration: 200ms; }
  /* Restore when cursor moves inside the ivory panel */
  .pa-drawer.is-open:has(.pa-drawer__panel:hover)::before { opacity: 0.6; transition-duration: 200ms; }

  /* Ivory content panel slides in from left */
  .pa-drawer__panel {
    position: absolute;
    top: 0;
    left: 0;
    width: 520px;
    height: 100%;
    background: var(--pa-ivory, #f9f8f4);
    color: var(--pa-ink, #1d1d1b);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    transform: translateX(-100%);
    transition: transform 420ms cubic-bezier(.22, 1, .36, 1);
  }
  .pa-drawer.is-open .pa-drawer__panel { transform: translateX(0); }

  /* ── Tab bar ── */
  .pa-drawer__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 20px 32px;
    border-bottom: 1px solid rgba(29, 29, 27, 0.1);
    flex-shrink: 0;
  }

  .pa-drawer__tabs {
    display: flex;
    align-items: center;
    gap: 24px;
    flex-wrap: wrap;
  }

  .pa-drawer__tab {
    font-family: var(--font-body, 'Instrument Sans', sans-serif);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(29, 29, 27, 0.35);
    background: none;
    border: none;
    border-bottom: 1px solid transparent;
    padding: 0 0 3px;
    cursor: pointer;
    transition: color 160ms ease, border-color 160ms ease;
    white-space: nowrap;
  }
  .pa-drawer__tab.is-active {
    color: var(--pa-ink, #1d1d1b);
    border-bottom-color: rgba(29, 29, 27, 0.5);
  }
  .pa-drawer__tab:hover { color: rgba(29, 29, 27, 0.65); }

  .pa-drawer__close {
    background: none;
    border: none;
    color: rgba(29, 29, 27, 0.45);
    cursor: pointer;
    padding: 0;
    line-height: 0;
    flex-shrink: 0;
    transition: color 160ms ease;
  }
  .pa-drawer__close:hover { color: var(--pa-ink, #1d1d1b); }

  /* ── Scrollable body ── */
  .pa-drawer__body {
    flex: 1;
    min-height: 0; /* required: flex items default to min-height:auto which prevents overflow-y from triggering */
    overflow-y: auto;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch; /* smooth momentum scroll on iOS */
  }

  /* ── Nav sections — one per top-level link ── */
  .pa-drawer__section { display: none; }
  .pa-drawer__section.is-active { display: block; }

  .pa-drawer__nav { padding: 24px 0 32px; }

  .pa-drawer__nav-link {
    display: block;
    padding: 11px 32px;
    font-family: var(--font-display, 'EB Garamond', Georgia, serif);
    font-weight: 400;
    font-size: clamp(24px, 3.2vw, 32px);
    line-height: 1.1;
    letter-spacing: -0.01em;
    color: var(--pa-ink, #1d1d1b);
    text-decoration: none;
    transition: opacity 160ms ease;
  }
  .pa-drawer__nav-link:hover { opacity: 0.5; }

  /* ── Search section ── */
  .pa-drawer__search { padding: 32px 32px 24px; }

  .pa-drawer__search-form {
    display: flex;
    align-items: center;
    gap: 12px;
    border-bottom: 1px solid rgba(29, 29, 27, 0.2);
    padding-bottom: 14px;
  }

  .pa-drawer__search-input {
    flex: 1;
    background: none;
    border: none;
    outline: none;
    font-family: var(--font-display, 'EB Garamond', Georgia, serif);
    font-size: clamp(24px, 3vw, 32px);
    color: var(--pa-ink, #1d1d1b);
  }
  .pa-drawer__search-input::placeholder { color: rgba(29, 29, 27, 0.3); }

  .pa-drawer__search-btn {
    background: none;
    border: none;
    color: var(--pa-ink, #1d1d1b);
    cursor: pointer;
    padding: 0;
    opacity: 0.45;
    transition: opacity 160ms ease;
    flex-shrink: 0;
  }
  .pa-drawer__search-btn:hover { opacity: 1; }

  /* ── Mobile footer (Account + Contact) ── */
  .pa-drawer__footer {
    display: none;
    align-items: center;
    gap: 32px;
    padding: 20px 32px;
    border-top: 1px solid rgba(29, 29, 27, 0.1);
    flex-shrink: 0;
  }

  .pa-drawer__footer-link {
    font-family: var(--font-body, 'Instrument Sans', sans-serif);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(29, 29, 27, 0.45);
    text-decoration: none;
    transition: color 160ms ease;
  }
  .pa-drawer__footer-link:hover { color: var(--pa-ink, #1d1d1b); }

  @media (max-width: 989px) {
    .pa-drawer__panel  { width: 86vw; }
    .pa-drawer__footer { display: flex; }
  }

  /* ================================================================
     Custom thin scrollbar — always visible, consistent across contexts
     ================================================================ */
  html {
    scrollbar-width: thin;
    scrollbar-color: rgba(29, 29, 27, 0.22) transparent;
  }
  ::-webkit-scrollbar        { width: 4px; }
  ::-webkit-scrollbar-track  { background: transparent; }
  ::-webkit-scrollbar-thumb  {
    background: rgba(29, 29, 27, 0.22);
    border-radius: 2px;
  }
  ::-webkit-scrollbar-thumb:hover { background: rgba(29, 29, 27, 0.42); }
</style>

<div id="pa-site-header" class="pa-header" role="banner">
  <div class="pa-header__inner">

    <!-- ── Row 1: utility nav ── -->
    <div class="pa-header__row--utility">

      <!-- Mobile: hamburger + search -->
      <div class="pa-header__mob-left">
        <button
          class="pa-header__link"
          type="button"
          aria-label="{{ 'sections.header.menu' | t }}"
          onclick="paOpenDrawer('nav')"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="1.5"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
            <line x1="4" x2="20" y1="6"  y2="6"/>
            <line x1="4" x2="20" y1="12" y2="12"/>
            <line x1="4" x2="20" y1="18" y2="18"/>
          </svg>
        </button>
        <button type="button" class="pa-header__link" aria-label="{{ 'general.search.search' | t }}" onclick="paOpenDrawer('search')">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="1.5"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
            <circle cx="11" cy="11" r="8"/>
            <path d="m21 21-4.3-4.3"/>
          </svg>
        </button>
      </div>

      <!-- Mobile: centred secondary logo -->
      <div class="pa-header__mob-logo">
        <a href="{{ routes.root_url }}" class="pa-header__logo-link">
          <img src="{{ 'secondary-logo-2-cocoa.svg' | asset_url }}"
               class="pa-header__logo pa-header__logo--dark"
               alt="{{ shop.name | escape }}"
               height="18"
               width="auto">
          <img src="{{ 'secondary-logo-2-ivory.svg' | asset_url }}"
               class="pa-header__logo pa-header__logo--light"
               alt="{{ shop.name | escape }}"
               height="18"
               width="auto">
        </a>
      </div>

      <!-- Desktop: primary nav + search -->
      <nav class="pa-header__nav" aria-label="Primary navigation">
        {%- for link in nav_links -%}
          {%- if link.links != empty -%}
            <button
              type="button"
              class="pa-header__link"
              onclick="paOpenDrawer('link-{{ forloop.index }}')"
            >{{ link.title }}</button>
          {%- else -%}
            <a
              href="{{ link.url }}"
              class="pa-header__link"
              {% if link.active %}aria-current="page"{% endif %}
            >{{ link.title }}</a>
          {%- endif -%}
        {%- endfor -%}

        <button type="button" class="pa-header__link" aria-label="{{ 'general.search.search' | t }}" onclick="paOpenDrawer('search')">
          <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="1.5"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
            <circle cx="11" cy="11" r="8"/>
            <path d="m21 21-4.3-4.3"/>
          </svg>
          Search
        </button>
      </nav>

      <!-- Desktop: utility + cart -->
      <div class="pa-header__util">
        <a href="{{ routes.root_url }}pages/contact" class="pa-header__link">Contact</a>

        {%- if shop.customer_accounts_enabled -%}
          <a
            href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
            class="pa-header__link"
            rel="nofollow"
          >Account</a>
        {%- endif -%}

        <a href="/pages/wishlist" class="pa-header__link">Wishlist (0)</a>

        <a href="{{ routes.cart_url }}" class="pa-header__link" id="pa-cart-icon">
          Bag{%- if cart.item_count > 0 -%}<span class="pa-header__cart-count"> ({{ cart.item_count }})</span>{%- endif -%}
        </a>
      </div>

      <!-- Mobile: wishlist + cart -->
      <div class="pa-header__mob-right">
        <a href="/pages/wishlist" class="pa-header__link" aria-label="Wishlist">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="1.5"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
            <path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 1.5C10.5 3.5 9.26 3 7.5 3A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/>
          </svg>
        </a>
        <a href="{{ routes.cart_url }}" class="pa-header__link" aria-label="{{ 'templates.cart.cart' | t }}">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="1.5"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
            <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/>
            <line x1="3" y1="6" x2="21" y2="6"/>
            <path d="M16 10a4 4 0 0 1-8 0"/>
          </svg>
          {%- if cart.item_count > 0 -%}
            <span class="pa-header__cart-count" aria-hidden="true">({{ cart.item_count }})</span>
          {%- endif -%}
        </a>
      </div>

    </div><!-- /row--utility -->

    <!-- ── Row 2: centred logo ── -->
    <div class="pa-header__row--logo">
      {%- if request.page_type == 'index' -%}
        <h1 class="pa-header__logo-wrap">
      {%- else -%}
        <div class="pa-header__logo-wrap">
      {%- endif -%}

        <a href="{{ routes.root_url }}" class="pa-header__logo-link">
          <img src="{{ 'primary-logo-cocoa.svg' | asset_url }}"
               class="pa-header__logo pa-header__logo--dark"
               alt="{{ shop.name | escape }}"
               height="18"
               width="auto">
          <img src="{{ 'primary-logo-ivory.svg' | asset_url }}"
               class="pa-header__logo pa-header__logo--light"
               alt="{{ shop.name | escape }}"
               height="18"
               width="auto">
        </a>

      {%- if request.page_type == 'index' -%}
        </h1>
      {%- else -%}
        </div>
      {%- endif -%}
    </div><!-- /row--logo -->

  </div><!-- /pa-header__inner -->
</div><!-- /pa-header -->

{%- comment -%}
  Navigation Drawer — reads from the same main-menu linklist as the header nav.
  Sub-menus are driven by nested links in Shopify Admin → Online Store → Navigation.

  Recommended main-menu structure to create in the Shopify admin:
  ┌─ Shop
  │   ├─ New arrivals          → /collections/new-arrivals
  │   ├─ Perfume               → /collections/perfume
  │   ├─ Home fragrance        → /collections/home-fragrance
  │   ├─ Candles               → /collections/candles
  │   └─ All products          → /collections/all
  ├─ Brands
  │   ├─ Atelier Maison Noir   → /collections/atelier-maison-noir
  │   ├─ House of Velour       → /collections/house-of-velour
  │   ├─ Studio Linnea         → /collections/studio-linnea
  │   ├─ Maison du Nord        → /collections/maison-du-nord
  │   └─ All brands            → /collections/all-brands
  └─ Discover
      ├─ The journal           -> /blogs/journal
      ├─ House studies         → /blogs/journal/tagged/house-study
      ├─ Field notes           → /blogs/journal/tagged/field-notes
      └─ About us              → /pages/about
{%- endcomment -%}

<!-- ── Navigation Drawer ── -->
<div class="pa-drawer" id="pa-drawer" role="dialog" aria-modal="true" aria-label="Navigation" aria-hidden="true">
  <div class="pa-drawer__panel">

  <div class="pa-drawer__head">
    <div class="pa-drawer__tabs">
      {%- for link in nav_links -%}
        <button
          class="pa-drawer__tab{% if forloop.first %} is-active{% endif %}"
          data-tab="link-{{ forloop.index }}"
          onclick="paDrawerTab('link-{{ forloop.index }}')"
        >{{- link.title -}}</button>
      {%- endfor -%}
      <button class="pa-drawer__tab" data-tab="search" onclick="paDrawerTab('search')">Search</button>
    </div>
    <button class="pa-drawer__close" type="button" aria-label="Close menu" onclick="paCloseDrawer()">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="1.5"
           stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M18 6 6 18M6 6l12 12"/>
      </svg>
    </button>
  </div>

  <div class="pa-drawer__body">

    <!-- One section per top-level nav item — sub-links shown as flat list -->
    {%- for link in nav_links -%}
      <div class="pa-drawer__section{% if forloop.first %} is-active{% endif %}"
           id="pa-drawer-link-{{ forloop.index }}">
        <nav class="pa-drawer__nav" aria-label="{{ link.title | escape }}">
          {%- for sub in link.links -%}
            <a
              href="{{ sub.url }}"
              class="pa-drawer__nav-link"
              {% if sub.active %}aria-current="page"{% endif %}
            >{{- sub.title -}}</a>
          {%- endfor -%}
        </nav>
      </div>
    {%- endfor -%}

    <!-- Search section -->
    <div class="pa-drawer__section" id="pa-drawer-search">
      <div class="pa-drawer__search">
        <form action="{{ routes.search_url }}" method="get" role="search">
          <input type="hidden" name="type" value="product">
          <div class="pa-drawer__search-form">
            <input
              id="pa-drawer-search-input"
              class="pa-drawer__search-input"
              type="search"
              name="q"
              placeholder="Search fragrances, houses, notes…"
              autocomplete="off"
              aria-label="{{ 'general.search.search' | t }}"
            >
            <button type="submit" class="pa-drawer__search-btn" aria-label="{{ 'general.search.search' | t }}">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
                   fill="none" stroke="currentColor" stroke-width="1.5"
                   stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <circle cx="11" cy="11" r="8"/>
                <path d="m21 21-4.3-4.3"/>
              </svg>
            </button>
          </div>
        </form>
      </div>
    </div>

  </div><!-- /pa-drawer__body -->

  <!-- Mobile footer: Account + Contact -->
  <div class="pa-drawer__footer">
    {%- if shop.customer_accounts_enabled -%}
      <a
        href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
        class="pa-drawer__footer-link"
        rel="nofollow"
      >Account</a>
    {%- endif -%}
    <a href="{{ routes.root_url }}pages/contact" class="pa-drawer__footer-link">Contact</a>
  </div>

  </div><!-- /pa-drawer__panel -->
</div><!-- /pa-drawer -->

<script>
  (function () {
    var header = document.getElementById('pa-site-header');
    if (!header) return;

    function setHeight() {
      var h = header.offsetHeight + 'px';
      document.documentElement.style.setProperty('--pa-header-height', h);
      document.documentElement.style.setProperty('--header-height', h);
    }

    function onScroll() {
      if (document.body.style.position === 'fixed') return; // drawer scroll-lock active — don't change state
      header.classList.toggle('is-scrolled', window.scrollY > 10);
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', setHeight);
    onScroll();
    setHeight();
  })();
</script>

<script>
  (function () {
    var drawer = document.getElementById('pa-drawer');
    if (!drawer) return;

    // Move to body — ensures position:fixed is viewport-relative even if a
    // Shopify section ancestor has a CSS transform.
    document.body.appendChild(drawer);

    var _scrollY = 0;

    function lockScroll() {
      _scrollY = window.scrollY;
      document.body.style.position   = 'fixed';
      document.body.style.top        = '-' + _scrollY + 'px';
      document.body.style.left       = '0';
      document.body.style.right      = '0';
      document.body.style.overflowY  = 'scroll'; // preserve scrollbar gutter
    }

    function unlockScroll() {
      document.body.style.position   = '';
      document.body.style.top        = '';
      document.body.style.left       = '';
      document.body.style.right      = '';
      document.body.style.overflowY  = '';
      window.scrollTo(0, _scrollY);
      // Re-evaluate header state now that scroll position is restored
      window.dispatchEvent(new Event('scroll'));
    }

    function openDrawer(tab) {
      drawer.classList.add('is-open');
      drawer.setAttribute('aria-hidden', 'false');
      lockScroll();
      if (tab === 'nav') {
        var first = drawer.querySelector('.pa-drawer__tab');
        tab = first ? first.dataset.tab : null;
      }
      if (tab) paDrawerTab(tab);
    }

    function closeDrawer() {
      drawer.classList.remove('is-open');
      drawer.setAttribute('aria-hidden', 'true');
      unlockScroll();
    }

    function paDrawerTab(tab) {
      drawer.querySelectorAll('.pa-drawer__tab').forEach(function (btn) {
        btn.classList.toggle('is-active', btn.dataset.tab === tab);
      });
      drawer.querySelectorAll('.pa-drawer__section').forEach(function (sec) {
        sec.classList.toggle('is-active', sec.id === 'pa-drawer-' + tab);
      });
      if (tab === 'search') {
        setTimeout(function () {
          var input = document.getElementById('pa-drawer-search-input');
          if (input) input.focus();
        }, 60);
      }
    }

    // Drawer covers the full viewport. A click on the pine overlay area
    // (outside the ivory panel) bubbles to the drawer and closes it.
    drawer.addEventListener('click', function (e) {
      if (!e.target.closest('.pa-drawer__panel')) closeDrawer();
    });

    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') closeDrawer();
    });

    window.paOpenDrawer  = openDrawer;
    window.paCloseDrawer = closeDrawer;
    window.paDrawerTab   = paDrawerTab;
  })();
</script>

<script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>

{%- if settings.cart_type == 'notification' -%}
  {%- render 'cart-notification',
    color_scheme: section.settings.color_scheme,
    desktop_menu_type: section.settings.menu_type_desktop
  -%}
{%- endif -%}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {%- assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' -%}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "max_blocks": 3,
  "settings": [
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_help.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "menu_color_scheme",
      "label": "t:sections.header.settings.menu_color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__utilities.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label",
      "info": "t:sections.header.settings.enable_country_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label",
      "info": "t:sections.header.settings.enable_language_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_customer_avatar",
      "default": true,
      "label": "t:sections.header.settings.enable_customer_avatar.label",
      "info": "t:sections.header.settings.enable_customer_avatar.info"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.header.settings.margin_bottom.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "@app"
    }
  ]
}
{% endschema %}
